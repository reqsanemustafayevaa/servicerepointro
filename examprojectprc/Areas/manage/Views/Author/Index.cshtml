@model List<Author>
@{
    int counter = 0;
}

<div class="d-flex justify-content-between align-items-center container-fluid">
    <h1>Genres</h1>
    <div> <a asp-action="create" asp-controller="author" class="btn btn-primary">Create</a></div>
</div>

<div class="container-fluid">
    <table class="table">
        <thead>
            <tr>

                <th scope="col">#</th>
                <th scope="col">FullName</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var author in Model)
            {
                counter++;
                <tr>
                    <th scope="row">@counter</th>
                    <td>@author.FullName</td>

                    <td>
                        <a asp-controller="author" asp-action="update" asp-route-id="@author.Id" class="btn btn-success">Update</a>
                        <a asp-controller="author" asp-action="delete" asp-route-id="@author.Id" class="btn btn-danger delete-btn">Delete</a>
                    </td>
                </tr>
            }

        </tbody>
    </table>
</div>

