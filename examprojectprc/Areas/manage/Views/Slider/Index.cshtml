@model List<Slider>
@{
    int counter = 0;
}

<div class="d-flex justify-content-between align-items-center container-fluid">
    <h1>Sliders</h1>
    <div> <a asp-action="create" asp-controller="slider" class="btn btn-primary">Create</a></div>
</div>

<div class="container-fluid">
    <table class="table">
        <thead>
            <tr>

                <th scope="col">#</th>
                <th scope="col">Title</th>
                <th scope="col">Description</th>
                <th scope="col">RedirectURL</th>
                <th scope="col">ImageUrl</th>
                <th scope="col">RedirecText</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var slider in Model)
            {
                counter++;
                <tr>
                    <th scope="row">@counter</th>
                    <td>@slider.Title</td>
                    <td>@slider.Description</td>
                    <td>@slider.RedirectUrl</td>
                    <td style="max-width:200px"><img src="~/uploads/sliders/@slider.ImageUrl" style=width:30%;height:30% /></td>
                    <td>@slider.RedirecText</td>
                    <td>
                        <a asp-controller="slider" asp-action="update" asp-route-id="@slider.Id" class="btn btn-success">Update</a>
                        <a asp-controller="slider" asp-action="delete" asp-route-id="@slider.Id" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            }

        </tbody>
    </table>
</div>


