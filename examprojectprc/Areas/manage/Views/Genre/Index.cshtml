@model List<Genre>
@{
    int counter = 0;
}

<div class="d-flex justify-content-between align-items-center container-fluid">
    <h1>Genres</h1>
    <div> <a asp-action="create" asp-controller="genre" class="btn btn-primary">Create</a></div>
</div>

<div class="container-fluid">
    <table class="table">
        <thead>
            <tr>

                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var genre in Model)
            {
                counter++;
                <tr>
                    <th scope="row">@counter</th>
                    <td>@genre.Name</td>

                    <td>
                        <a asp-controller="genre" asp-action="update" asp-route-id="@genre.Id" class="btn btn-success">Update</a>
                        <a asp-controller="genre" asp-action="delete" asp-route-id="@genre.Id" class="btn btn-danger delete-btn">Delete</a>
                    </td>
                </tr>
            }

        </tbody>
    </table>
</div>

